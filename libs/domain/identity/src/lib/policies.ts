/** Minimum plaintext password length for new credentials. */
export const MIN_PASSWORD_LENGTH = 8;

/** Absolute session lifetime from creation (30 days). */
export const SESSION_ABSOLUTE_TTL_MS = 30 * 24 * 60 * 60 * 1000;
